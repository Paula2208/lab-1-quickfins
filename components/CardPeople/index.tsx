type DashboardProps = {
    children: React.ReactNode,
};

export default function Dashboard({ children }: DashboardProps) {
    return (
        <></>
        // <Card>
            
        // </Card>
    );
}