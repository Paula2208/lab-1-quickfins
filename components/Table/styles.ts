import { styled } from "styled-components";

export const TableWrapper = styled.div`
  max-height: 150px;
  min-height: 95px;
  overflow: scroll;
  border-radius: 10px;
  border: 1px solid var(--violet);
  display: flex;
  width: calc(100% - 2px);
`;