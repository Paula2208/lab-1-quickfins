import { styled } from "styled-components";

export const TableWrapper = styled.div`
  height: 150px;
  min-height: 150px;
  overflow: scroll;
  border-radius: 10px;
  border: 1px solid var(--violet);
  display: flex;
  width: calc(100% - 2px);
`;