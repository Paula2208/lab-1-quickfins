export default interface RouteTitles {
    label: string;
    route: string;
}

export interface TableItem {
    id: string;
    labels: string[];
}

export interface SelectItem {
    value: number;
    label: string;
}